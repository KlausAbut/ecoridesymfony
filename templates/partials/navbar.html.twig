<header>
  <nav class="navbar navbar-expand-lg navbar-light bg-light fixed-top">
    <div class="container">
      <a class="navbar-brand" href="{{ path('index') }}">
        <span class="green">Eco</span>Ride
      </a>

      <button
        class="navbar-toggler"
        type="button"
        data-bs-toggle="collapse"
        data-bs-target="#mainNavbar"
        aria-controls="mainNavbar"
        aria-expanded="false"
        aria-label="Toggle navigation"
      >
        <span class="navbar-toggler-icon"></span>
      </button>

      <div class="collapse navbar-collapse" id="mainNavbar">
        <!-- Menu principal : empilé verticalement sous 992px -->
        <ul class="navbar-nav flex-column flex-lg-row w-100">
          <li class="nav-item">
            <a class="nav-link py-2 py-lg-0" href="{{ path('index') }}">Accueil</a>
          </li>
          <li class="nav-item">
            <a class="nav-link py-2 py-lg-0" href="{{ path('covoiturage_list') }}">Covoiturages</a>
          </li>
          <li class="nav-item">
            <a class="nav-link py-2 py-lg-0" href="{{ path('contact') }}">Contact</a>
          </li>
          <li class="nav-item">
            <a class="nav-link py-2 py-lg-0" href="{{ path('about') }}">À propos</a>
          </li>
          <li class="nav-item">
            <a class="nav-link py-2 py-lg-0" href="{{ path('avis_list') }}">Avis</a>
          </li>

          {# À partir de lg, on pousse ces liens à droite #}
          <li class="nav-item mt-2 mt-lg-0 ms-lg-auto">
            {% if app.user %}
              <div class="dropdown">
                <a
                  class="nav-link dropdown-toggle px-0"
                  href="#"
                  id="userDropdown"
                  role="button"
                  data-bs-toggle="dropdown"
                  aria-expanded="false"
                >Mon compte</a>
                <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
                  {% if 'ROLE_ADMIN' in app.user.roles %}
                    <li><a class="dropdown-item" href="{{ path('admin_dashboard') }}">⚙️ Admin</a></li>
                  {% elseif 'ROLE_EMPLOYE' in app.user.roles %}
                    <li><a class="dropdown-item" href="{{ path('employe_moderation') }}">🛠️ Employé</a></li>
                  {% else %}
                    <li><a class="dropdown-item" href="{{ path('user_profile') }}">Mon profil</a></li>
                    <li><a class="dropdown-item" href="{{ path('user_reservations') }}">Mes réservations</a></li>
                    <li><a class="dropdown-item" href="{{ path('user_trajets') }}">Mes trajets</a></li>
                  {% endif %}
                  <li><hr class="dropdown-divider"></li>
                  <li><a class="dropdown-item text-danger" href="{{ path('app_logout') }}">Déconnexion</a></li>
                </ul>
              </div>
            {% else %}
              <a class="btn btn-success btn-sm me-2" href="{{ path('app_login') }}">Connexion</a>
              <a class="btn btn-success btn-sm" href="{{ path('app_register') }}">Inscription</a>
            {% endif %}
          </li>
        </ul>
      </div>
    </div>
  </nav>
</header>