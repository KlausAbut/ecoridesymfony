{% extends 'base.html.twig' %}

{% block title %}Mes voitures — EcoRide{% endblock %}

{% block body %}
<div class="page-wrapper">

  <div class="d-flex justify-content-between align-items-center mb-4 flex-wrap gap-2">
    <div>
      <h1 class="mb-0" style="font-size: 1.8rem;">🚘 Mes voitures</h1>
      <p class="text-muted mb-0" style="font-size: 0.9rem;">Vos véhicules enregistrés</p>
    </div>
    <a href="{{ path('voiture_create') }}" class="btn btn-success">➕ Ajouter une voiture</a>
  </div>

  {% if voitures is empty %}
    <div class="card p-5 text-center">
      <div style="font-size: 3rem; margin-bottom: 1rem;">🚗</div>
      <h5 class="mb-2">Aucune voiture enregistrée</h5>
      <p class="text-muted mb-4">Ajoutez votre premier véhicule pour commencer à proposer des trajets.</p>
      <a href="{{ path('voiture_create') }}" class="btn btn-success mx-auto" style="max-width: 220px;">➕ Ajouter une voiture</a>
    </div>
  {% else %}
    <div class="d-flex flex-column gap-3">
      {% for voiture in voitures %}
        <div class="card p-3">
          <div class="d-flex justify-content-between align-items-center flex-wrap gap-3">
            <div class="d-flex align-items-center gap-3">
              <div class="rounded-circle d-flex align-items-center justify-content-center flex-shrink-0"
                   style="width:48px;height:48px;background:var(--eco-mint);font-size:1.4rem;border:2px solid var(--eco-secondary);">🚘</div>
              <div>
                <div class="fw-semibold">{{ voiture.modele }}</div>
                <div class="text-muted" style="font-size: 0.82rem;">
                  {{ voiture.immatriculation }}
                  &nbsp;·&nbsp;
                  {% if voiture.energie == 'électrique' %}
                    <span style="color: var(--eco-secondary);">⚡ Électrique</span>
                  {% else %}
                    {{ voiture.energie|capitalize }}
                  {% endif %}
                </div>
              </div>
            </div>
            <div class="d-flex gap-2">
              <a href="{{ path('voiture_edit', {'id': voiture.id}) }}" class="btn btn-sm btn-outline-primary">✏️ Modifier</a>
              {% if is_granted('ROLE_ADMIN') %}
                <a href="{{ path('voiture_delete', {'id': voiture.id}) }}" class="btn btn-sm btn-outline-danger"
                   x-data
                   @click.prevent="if(confirm('Supprimer cette voiture ?')) window.location.href = $el.href">
                  🗑️ Supprimer
                </a>
              {% endif %}
            </div>
          </div>
        </div>
      {% endfor %}
    </div>
  {% endif %}

</div>
{% endblock %}
