<header>
 <nav class="navbar navbar-expand-lg navbar-light bg-white fixed-top shadow-sm">
  <div class="container">
    {# 1) Brand (logo) #}
    <a class="navbar-brand" href="{{ path('index') }}">
      <span class="green">Eco</span>Ride
    </a>

    {# 2) Bouton “hamburger” #}
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#mainNavbar"
      aria-controls="mainNavbar"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>

    {# 3) Contenu “collapsé” (sera caché sous 992px) #}
    <div class="collapse navbar-collapse" id="mainNavbar">
      {# 3a) Liens principaux, alignés à gauche #}
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link py-1" href="{{ path('index') }}">Accueil</a>
        </li>
        <li class="nav-item">
          <a class="nav-link py-1" href="{{ path('covoiturage_list') }}">Covoiturages</a>
        </li>
        <li class="nav-item">
          <a class="nav-link py-1" href="{{ path('contact') }}">Contact</a>
        </li>
        <li class="nav-item">
          <a class="nav-link py-1" href="{{ path('about') }}">À propos</a>
        </li>
        <li class="nav-item">
          <a class="nav-link py-1" href="{{ path('avis_list') }}">Avis</a>
        </li>
      </ul>

      {# 3b) Partie droite : Connexion/Inscription ou Menu utilisateur #}
      <ul class="navbar-nav mb-2 mb-lg-0">
        {% if app.user %}
          <li class="nav-item dropdown">
            <a
              class="nav-link dropdown-toggle py-1"
              href="#"
              id="userDropdown"
              role="button"
              data-bs-toggle="dropdown"
              aria-expanded="false"
            >
              Mon compte
            </a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
              {% if 'ROLE_ADMIN' in app.user.roles %}
                <li>
                  <a class="dropdown-item" href="{{ path('admin_dashboard') }}">⚙️ Admin</a>
                </li>
              {% elseif 'ROLE_EMPLOYE' in app.user.roles %}
                <li>
                  <a class="dropdown-item" href="{{ path('employe_moderation') }}">🛠️ Employé</a>
                </li>
              {% else %}
                <li>
                  <a class="dropdown-item" href="{{ path('user_profile') }}">Mon profil</a>
                </li>
                <li>
                  <a class="dropdown-item" href="{{ path('user_reservations') }}">Mes réservations</a>
                </li>
                <li>
                  <a class="dropdown-item" href="{{ path('user_trajets') }}">Mes trajets</a>
                </li>
              {% endif %}
              <li><hr class="dropdown-divider"></li>
              <li>
                <a class="dropdown-item text-danger" href="{{ path('app_logout') }}">Déconnexion</a>
              </li>
            </ul>
          </li>
        {% else %}
          <li class="nav-item me-2">
            <a class="btn btn-success btn-sm" href="{{ path('app_login') }}">Connexion</a>
          </li>
          <li class="nav-item">
            <a class="btn btn-success btn-sm" href="{{ path('app_register') }}">Inscription</a>
          </li>
        {% endif %}
      </ul>
    </div>
  </div>
</nav>
</header>