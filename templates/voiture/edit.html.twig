{% extends 'base.html.twig' %}

{% form_theme form with [
    'form/template.html.twig'
] %}


{% block body %}

    <h2>{{ is_create ? 'Ajouter une voiture' : 'Modifier cet voiture' }}</h2>

    {{ form_start(form) }}

    {{ form_row(form.modele) }}
    {{ form_row(form.immatriculation) }}
    {{ form_row(form.energie) }}
    
    {{ form_end(form) }}

{% endblock %} 